
  # Responsive Layout Patterns Library

  This is a code bundle for Responsive Layout Patterns Library. The original project is available at https://www.figma.com/design/foWLEKq2biHRc0hnRtaA1v/Responsive-Layout-Patterns-Library.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  